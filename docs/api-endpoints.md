# API Endpoints

Branch: `feature/api-endpoints`  
Related Issue: #3

## Endpoints
- GET `/api/health` — returns status and timestamp
- GET `/api/version` — returns app name and version
- POST `/api/echo` — returns the JSON payload back with a timestamp

## Notes
Demo endpoints for coursework. No data persistence.
